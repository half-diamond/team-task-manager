{
  "name": "team-task-manager",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace backend",
    "start": "npm start --workspace backend"
  }
}
